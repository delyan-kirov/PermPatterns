language ESSENCE 2.4.0
$ mesh param file

$ mesh_avoidance = permutation + mesh pieces + vincular indeces
letting mixed_avoidance be { ( sequence(1,3,2), relation( ), {1}, {0,2} ) } 
letting length be 4

$ n Solution for avoidance should be:
$ Avoiding pattern
$ test

$ Contains pattern but breaks mesh
$ test
